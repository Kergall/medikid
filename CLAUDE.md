# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository is in its initial stage. As of the latest commit, it contains a single `index.html` file that appears to be corrupted (null bytes only). There is no build system, source code, dependencies, or configuration in place yet.

**Repository**: `Kergall/medikid`
**Project name**: medikid (likely a medical/health application for children, based on the name)

## Next Steps Expected

Before meaningful development can begin, the following will likely need to be established:

- A working `index.html` or scaffolded project (the existing file is corrupted and should be replaced)
- A package manager and build tooling (e.g., `package.json` with npm/yarn/pnpm)
- Source files and project structure

Once the project is scaffolded, this file should be updated to document:
- Build, dev server, test, and lint commands
- Project architecture and key modules
- Conventions and patterns in use
